kind = "web"
previewPath = "/"
title = "Trail Connect"
version = "1.0.0"
id = "artifacts/trail-connect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20387

[services.development]
run = "pnpm --filter @workspace/trail-connect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trail-connect", "run", "build" ]
publicDir = "artifacts/trail-connect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20387"
BASE_PATH = "/"
